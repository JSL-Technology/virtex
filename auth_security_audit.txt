AUDITORÍA EXHAUSTIVA DE SEGURIDAD DE AUTENTICACIÓN Y AUTORIZACIÓN
Repositorio: /workspace/virtex
Fecha: 2026-06-01
Auditor: Ingeniero senior de seguridad / arquitecto fullstack
Alcance: EXCLUSIVAMENTE autenticación, autorización, usuarios, roles, tokens y sesiones en frontend y backend.

================================================================================
1. RESUMEN EJECUTIVO
================================================================================

Veredicto: el módulo NO está listo para producción a escala empresarial con el estándar de Auth0, GitHub o Stripe.

El backend contiene varias decisiones sólidas: cookies httpOnly para tokens, CSRF firmado de doble envío, JWT de acceso RS256 con kid, refresh tokens hasheados y rotación transaccional, validación global estricta y redacción de secretos en logs. Sin embargo, existen defectos funcionales y de seguridad que impedirían aprobarlo en un comité de seguridad empresarial: el endpoint /auth/status verifica access tokens con JWT_SECRET aunque los access tokens se emiten en RS256; el logout frontend usa sendBeacon sin header CSRF y puede no revocar la sesión en servidor; la edición administrativa de usuarios permite cambiar emails sin flujo de verificación; el frontend no envía currentPassword al habilitar 2FA aunque el backend lo exige; y el DTO OpenAPI aún documenta tokens en cuerpo pese a que el contrato real dice cookie-only.

La arquitectura muestra intención de defensa en profundidad, pero todavía hay inconsistencias entre frontend y backend y problemas de ciclo de vida de sesión que son críticos en autenticación empresarial. Las áreas de mayor riesgo son consistencia de sesión, cambios de identidad/email, step-up MFA y contratos públicos de API.

================================================================================
2. TABLA DE CALIFICACIONES
================================================================================

BACKEND

| Dimensión | Puntaje | Justificación con evidencia |
|---|---:|---|
| Seguridad | 7/10 | Puntos fuertes: cookies httpOnly/SameSite en apps/backend/api/src/app/auth/services/cookie.service.ts:21-38, CSRF firmado en apps/backend/api/src/app/auth/guards/csrf.guard.ts:24-66, access JWT RS256 con issuer/audience en apps/backend/api/src/app/auth/strategies/jwt.strategy/jwt.strategy.ts:37-67. Penaliza fuerte: /auth/status usa JWT_SECRET en apps/backend/api/src/app/auth/services/session.service.ts:374-378 aunque TokenService firma access tokens con RS256 en apps/backend/api/src/app/auth/services/token.service.ts:223-244; edición admin permite cambiar email sin verificación en apps/backend/api/src/app/users/dto/update-user.dto.ts:12-14 y apps/backend/api/src/app/users/users.service.ts:130-149. |
| Arquitectura | 7/10 | Hay separación AuthService/SessionService/TokenService y cache/circuit breaker en apps/backend/api/src/app/auth/strategies/jwt.strategy/jwt.strategy.ts:69-83. Penaliza: duplicación/confusión de verificación JWT entre JwtStrategy y SessionService.verifyUserFromToken en apps/backend/api/src/app/auth/services/session.service.ts:374-378. |
| Validación | 8/10 | ValidationPipe global con whitelist/forbidNonWhitelisted/transform en apps/backend/api/src/main.ts:78-87; DTOs con IsEmail/MaxLength/Matches en apps/backend/api/src/app/auth/dto/register-user.dto.ts:47-64. Penaliza: UpdateUserDto permite email genérico en apps/backend/api/src/app/users/dto/update-user.dto.ts:12-14; enable2fa frontend/backend inconsistente. |
| Manejo de errores | 7/10 | Reset/forgot password evita enumeración con mensaje genérico en apps/backend/api/src/app/auth/auth.controller.ts:303-314 y apps/backend/api/src/app/auth/services/password-recovery.service.ts:30-35. Penaliza: errores específicos de usuarios internos/organizacionales aparecen en varios NotFoundException, por ejemplo apps/backend/api/src/app/users/users.service.ts:124-127 y 191-193, aceptable para usuarios autorizados pero no ideal como patrón global. |
| Escalabilidad | 7/10 | Throttler Redis en producción en apps/backend/api/src/app/app.module.ts:181-203, cache de usuario y fallback a DB con circuit breaker en apps/backend/api/src/app/auth/strategies/jwt.strategy/jwt.strategy.ts:90-137. Penaliza: logout global por usuario revoca todas las sesiones en apps/backend/api/src/app/auth/auth.service.ts:168-171 y apps/backend/api/src/app/auth/services/session.service.ts:363-371; no existe endpoint claro de logout sólo sesión actual. |
| Observabilidad | 8/10 | Pino redacta authorization, cookie, password, token, code y recaptchaToken en apps/backend/api/src/app/app.module.ts:130-149; refresh emite auditoría minimizando PII en apps/backend/api/src/app/auth/services/session.service.ts:253-267. Penaliza: algunos logs internos contienen userId/org en claro, aunque no exponen contraseñas/tokens. |

FRONTEND

| Dimensión | Puntaje | Justificación con evidencia |
|---|---:|---|
| Seguridad del cliente | 7/10 | No almacena access/refresh token en localStorage; AuthService modela LoginResponse sólo con user en apps/core/client-web/src/app/core/services/auth.ts:30-36 y usa withCredentials en login/refresh en apps/core/client-web/src/app/core/services/auth.ts:123-149. Penaliza: logout con sendBeacon no adjunta X-XSRF-TOKEN y puede saltarse revocación si sendBeacon devuelve true en apps/core/client-web/src/app/core/services/auth.ts:321-355. |
| Manejo de tokens/sesión | 6/10 | Interceptor añade X-XSRF-TOKEN y coordina un único refresh en apps/core/client-web/src/app/core/interceptors/auth.interceptor.ts:26-37 y 46-125. Penaliza: el endpoint /auth/status del backend está roto para RS256; enable2fa no manda currentPassword en apps/core/client-web/src/app/core/services/auth.ts:228-230; logout sendBeacon no es compatible con CsrfGuard. |
| Protección de rutas | 8/10 | authGuard espera checkAuthStatus en apps/core/client-web/src/app/core/guards/auth-guard.ts:19-31; permissionsGuard espera auth pending antes de evaluar permisos en apps/core/client-web/src/app/core/guards/permissions-guard.ts:19-28; rutas sensibles de settings usan permissionsGuard en apps/core/client-web/src/app/features/settings/settings.routes.ts:111-123. |
| Consistencia con backend | 5/10 | Contrato cookie-only está alineado en login/refresh/register, pero hay inconsistencias importantes: frontend enable2fa omite currentPassword requerido por backend en apps/backend/api/src/app/auth/dto/enable-2fa.dto.ts:13-19; AuthResponseDto aún documenta accessToken/refreshToken en apps/backend/api/src/app/auth/dto/auth-response.dto.ts:3-11; sendBeacon logout no cumple CsrfGuard. |

PUNTUACIÓN GLOBAL: 6.8/10

Justificación global: la base criptográfica y de cookies está por encima del promedio, pero las inconsistencias de sesión, estado de autenticación y cambios de identidad no pasarían un estándar empresarial estricto. No se redondea hacia arriba: los defectos señalados afectan disponibilidad, revocación y seguridad de identidad.

================================================================================
3. FORTALEZAS — LO QUE ESTÁ BIEN IMPLEMENTADO
================================================================================

F1 — Tokens en cookies httpOnly con flags razonables
Archivo: apps/backend/api/src/app/auth/services/cookie.service.ts
Líneas: 21-38
Código actual:
    const baseOptions = {
      httpOnly: true,
      secure: isProduction,
      sameSite: 'lax' as 'strict' | 'lax' | 'none',
      path: '/',
    };

    res.cookie(accessTokenName, accessToken, {
      ...baseOptions,
      maxAge: AuthConfig.COOKIE_ACCESS_MAX_AGE,
    });

Por qué está bien: los tokens no están expuestos a JavaScript, reduciendo exfiltración por XSS. En producción usa prefijos __Host-/__Secure- y secure=true.
Referencia: OWASP Session Management Cheat Sheet; OWASP HTML5 Security Cheat Sheet; RFC 6265bis cookie prefixes.

F2 — CSRF con doble envío firmado y comparación timing-safe
Archivo: apps/backend/api/src/app/auth/guards/csrf.guard.ts
Líneas: 24-66
Código actual:
    const headerValue = request.headers['x-xsrf-token'] as string | undefined;
    const cookieValue = request.cookies['XSRF-TOKEN'] as string | undefined;
    if (!headerValue || !cookieValue || headerValue !== cookieValue) {
      ...
      throw new ForbiddenException('Invalid CSRF Token');
    }
    ...
    const expectedMac = crypto
      .createHmac('sha256', AuthConfig.CSRF_SECRET)
      .update(nonce)
      .digest('base64url');
    ...
    if (expectedBuf.length !== receivedBuf.length || !crypto.timingSafeEqual(expectedBuf, receivedBuf)) {
      ...
      throw new ForbiddenException('Invalid CSRF Token');
    }

Por qué está bien: el patrón firmado evita que una cookie XSRF fijada por subdominio sea aceptada sin conocer el secreto HMAC.
Referencia: OWASP CSRF Prevention Cheat Sheet, Signed Double-Submit Cookie; CWE-352.

F3 — JWT de acceso con RS256, kid, issuer y audience
Archivo: apps/backend/api/src/app/auth/strategies/jwt.strategy/jwt.strategy.ts
Líneas: 37-67
Código actual:
    secretOrKeyProvider: async (_req: Request, rawJwt: string, done: ... ) => {
      ...
      const header = JSON.parse(Buffer.from(parts[0], 'base64url').toString('utf8'));
      const publicKey = keyManagementService.getPublicKey(header.kid);
      ...
    },
    algorithms: ['RS256'],
    issuer: 'virteex-api',
    audience: 'virteex-web',

Archivo: apps/backend/api/src/app/auth/services/token.service.ts
Líneas: 236-244
Código actual:
    const { kid, privateKey } = this.keyManagementService.getActiveKey();
    return this.jwtService.sign(payload, {
      privateKey,
      expiresIn: expiresIn || AuthConfig.JWT_ACCESS_EXPIRATION,
      algorithm: 'RS256',
      keyid: kid,
      issuer: 'virteex-api',
      audience: 'virteex-web',
    } as any);

Por qué está bien: RS256 con kid facilita rotación de llaves y reduce riesgo operacional frente a secretos simétricos compartidos.
Referencia: RFC 7515 (JWS kid); RFC 7519 (JWT iss/aud); OWASP JWT Cheat Sheet.

F4 — Refresh token hasheado en base de datos y rotación transaccional
Archivo: apps/backend/api/src/app/auth/services/token.service.ts
Líneas: 204-213
Código actual:
    const refreshTokenPayload = { ...payload, jti: refreshTokenRecord.id };
    const refreshToken = this.getJwtToken(
      refreshTokenPayload,
      refreshExpiration,
      this.configService.get('JWT_REFRESH_SECRET')
    );
    const tokenHash = crypto.createHash('sha256').update(refreshToken).digest('hex');
    await this.refreshTokenRepository.update(refreshTokenRecord.id, { tokenHash });

Archivo: apps/backend/api/src/app/auth/services/session.service.ts
Líneas: 121-145
Código actual:
    const lockResult = await this.dataSource.transaction('SERIALIZABLE', async (em) => {
      const rt = await em
        .getRepository(RefreshToken)
        .createQueryBuilder('rt')
        .setLock('pessimistic_write')
        .where('rt.id = :jti AND rt.userId = :userId', { jti: payload.jti, userId: payload.id })
        ...
        .getOne();
      if (!rt || rt.isRevoked || rt.expiresAt <= new Date() || rt.tokenHash !== currentTokenHash) {
        return { entity: rt, rotated: false };
      }
      rt.isRevoked = true;
      rt.revokedAt = new Date();
      await em.save(RefreshToken, rt);
      return { entity: rt, rotated: true };
    });

Por qué está bien: comprometer la DB no revela refresh tokens en claro; la rotación bajo lock reduce carreras y replay concurrente.
Referencia: OAuth 2.0 Security Best Current Practice, refresh token rotation/reuse detection; OWASP ASVS 3.3.3/3.3.4; CWE-613/CWE-362.

F5 — Validación global estricta de entrada
Archivo: apps/backend/api/src/main.ts
Líneas: 78-87
Código actual:
    app.useGlobalPipes(
      new ValidationPipe({
        whitelist: true,
        forbidNonWhitelisted: true,
        transform: true,
        transformOptions: {
          enableImplicitConversion: true,
        },
      }),
    );

Por qué está bien: bloquea propiedades inesperadas y aplica DTOs en runtime, reduciendo mass assignment y bypass por campos extra.
Referencia: OWASP API Security 2023 API3 Broken Object Property Level Authorization; CWE-915.

F6 — Recuperación de contraseña evita enumeración y usa token aleatorio hasheado con expiración
Archivo: apps/backend/api/src/app/auth/services/password-recovery.service.ts
Líneas: 30-50
Código actual:
    const genericMessage = 'Si existe una cuenta con ese correo, se ha enviado un enlace para restablecer la contraseña.';
    const user = await this.userRepository.findOne({ where: { email }, relations: ['security'] });
    if (!user) {
      await this.simulateDelay();
      return { message: genericMessage };
    }
    const rawToken = crypto.randomBytes(32).toString('hex');
    const hashedToken = crypto.createHash('sha256').update(rawToken).digest('hex');
    ...
    user.security.passwordResetExpires = new Date(Date.now() + this.convertToMs(expirationTime));

Por qué está bien: no revela existencia de cuenta, guarda hash del token y aplica TTL.
Referencia: OWASP Forgot Password Cheat Sheet; CWE-203; CWE-640.

F7 — Logging con redacción explícita de secretos
Archivo: apps/backend/api/src/app/app.module.ts
Líneas: 130-149
Código actual:
    redact: {
      paths: [
        'req.headers.authorization',
        'req.headers.cookie',
        'req.body.password',
        'req.body.token',
        'req.body.code',
        'req.body.recaptchaToken',
        'req.body.currentPassword',
        'req.body.newPassword',
      ],
      censor: '[REDACTED]',
    },

Por qué está bien: reduce exposición accidental de credenciales y tokens en logs operacionales.
Referencia: OWASP Logging Cheat Sheet; CWE-532.

F8 — Guards de rutas en frontend esperan estado auth y permisos
Archivo: apps/core/client-web/src/app/core/guards/auth-guard.ts
Líneas: 19-31
Código actual:
    if (authService.isAuthenticated()) {
      return of(true);
    }
    return authService.checkAuthStatus().pipe(
      take(1),
      map(isAuthenticated => {
        if (isAuthenticated) {
          return true;
        }
        const lang = languageService.currentLang() || 'es';
        return router.createUrlTree(['/', lang, 'auth', 'login']);
      })
    );

Archivo: apps/core/client-web/src/app/core/guards/permissions-guard.ts
Líneas: 19-28
Código actual:
    if (authService.authStatus() === AuthStatus.pending) {
      return authService.checkAuthStatus().pipe(map(() => decide()));
    }
    return decide();

Por qué está bien: evita evaluar permisos sobre un usuario aún no cargado y reduce race conditions de UI.
Referencia: OWASP ASVS V4 Access Control; CWE-362.

================================================================================
4. TABLA RESUMEN DE HALLAZGOS NEGATIVOS
================================================================================

| Severidad | Tipo | Capa | Archivo | Línea | Resumen |
|---|---|---|---|---:|---|
| Crítica | Bug de autenticación/sesión | Backend | apps/backend/api/src/app/auth/services/session.service.ts | 374-378 | /auth/status verifica el access token con JWT_SECRET, pero los access tokens se firman con RS256. El estado auth falla tras recarga y rompe guards/sesión. |
| Alta | Revocación de sesión rota | Frontend/Backend | apps/core/client-web/src/app/core/services/auth.ts | 321-355 | logout usa sendBeacon sin X-XSRF-TOKEN contra endpoint protegido por CsrfGuard; si sendBeacon devuelve true no hay fallback y la sesión servidor puede quedar viva. |
| Alta | Cambio de identidad/email sin verificación | Backend | apps/backend/api/src/app/users/dto/update-user.dto.ts | 12-14 | UpdateUserDto permite email en edición admin; UsersService aplica Object.assign y sólo invalida tokenVersion si cambia roleId. |
| Alta | Inconsistencia MFA step-up | Frontend/Backend | apps/core/client-web/src/app/core/services/auth.ts | 228-230 | Frontend enable2fa envía sólo token, pero backend EnableTwoFactorDto exige currentPassword; el flujo 2FA está roto o invita a relajar el backend. |
| Media | Contrato API inseguro/engañoso | Backend | apps/backend/api/src/app/auth/dto/auth-response.dto.ts | 3-11 | Swagger/AuthResponseDto aún declara accessToken y refreshToken en cuerpo, contradiciendo la política cookie-only. |
| Media | Logout semánticamente excesivo | Backend | apps/backend/api/src/app/auth/auth.service.ts | 168-171 | /auth/logout invoca terminateAllSessions; no hay logout sólo de sesión actual aunque existe gestión de sesiones. |
| Media | Reintentos automáticos en refresh pueden amplificar side effects | Frontend | apps/core/client-web/src/app/core/interceptors/auth.interceptor.ts | 53-72 | Retry envuelve el refresh endpoint; sólo filtra idempotencia para 5xx/status0, pero el refresh token rotation no debe reintentarse sin diseño idempotente explícito. |
| Baja | Registro acepta teléfono sin validador E.164 | Backend | apps/backend/api/src/app/auth/dto/register-user.dto.ts | 87-90 | phone de registro es string opcional sin IsE164PhoneNumber, a diferencia de updateProfile y sendPhoneOtp. |
| Baja | Tipos y providers duplicados/confusos | Frontend | apps/core/client-web/src/app/app.config.ts | 38,93-99 | provideHttpClient y provideServiceWorker están registrados más de una vez; no es exploit directo, pero aumenta incertidumbre en interceptores/auth init. |

================================================================================
5. HALLAZGOS DETALLADOS
================================================================================

--------------------------------------------------------------------------------
H1 — CRÍTICA — /auth/status no puede verificar access tokens RS256
--------------------------------------------------------------------------------

Evidencia — código actual:
Archivo: apps/backend/api/src/app/auth/auth.controller.ts
Líneas: 276-287
    @Get('status')
    @Public()
    @Header('Cache-Control', 'no-store')
    async checkAuthStatus(@Req() req: Request) {
      const token = req.cookies['__Host-access_token'] || req.cookies['access_token'];
      ...
      const user = await this.authService.verifyUserFromToken(token);

Archivo: apps/backend/api/src/app/auth/services/session.service.ts
Líneas: 374-378
    async verifyUserFromToken(token: string): Promise<User | null> {
      try {
        const payload = this.jwtService.verify(token, {
          secret: this.configService.getOrThrow('JWT_SECRET'),
        });

Archivo: apps/backend/api/src/app/auth/services/token.service.ts
Líneas: 236-244
    const { kid, privateKey } = this.keyManagementService.getActiveKey();
    return this.jwtService.sign(payload, {
      privateKey,
      expiresIn: expiresIn || AuthConfig.JWT_ACCESS_EXPIRATION,
      algorithm: 'RS256',
      keyid: kid,
      issuer: 'virteex-api',
      audience: 'virteex-web',
    } as any);

Explicación:
El endpoint de status lee la cookie de access token, pero llama a verifyUserFromToken que usa JWT_SECRET simétrico. El emisor real usa RS256 con privateKey, kid, issuer y audience. Resultado esperado: los access tokens válidos fallan en /auth/status, checkAuthStatus devuelve false, authGuard redirige a login después de recarga y el cliente pierde estado aunque la cookie sea válida. En un sistema empresarial esto es inaceptable porque el endpoint de introspección/estado debe usar exactamente las mismas reglas de validación que el guard JWT.

Corrección propuesta:
    // Opción A: reutilizar JwtStrategy/KeyManagementService en SessionService
    async verifyUserFromToken(token: string): Promise<User | null> {
      try {
        const decoded = this.jwtService.decode(token, { complete: true }) as { header?: { kid?: string } } | null;
        const publicKey = this.keyManagementService.getPublicKey(decoded?.header?.kid);
        if (!publicKey) return null;

        const payload = this.jwtService.verify<JwtPayload>(token, {
          publicKey,
          algorithms: ['RS256'],
          issuer: 'virteex-api',
          audience: 'virteex-web',
        } as any);

        const user = await this.usersService.findUserByIdForAuth(payload.id);
        if (!user || user.status !== UserStatus.ACTIVE || (user.security?.tokenVersion ?? 0) !== payload.tokenVersion) return null;
        return user;
      } catch {
        return null;
      }
    }

    // Opción B preferida: eliminar verificación duplicada y hacer /status protegido por JwtAuthGuard:
    @Get('status')
    @UseGuards(JwtAuthGuard)
    @Header('Cache-Control', 'no-store')
    async checkAuthStatus(@CurrentUser() user: AuthenticatedUser) { ... }

Referencia:
RFC 7519 exige validar firma y claims críticos de forma consistente; OWASP JWT Cheat Sheet recomienda restringir algoritmo y validar issuer/audience; CWE-347 Improper Verification of Cryptographic Signature.

--------------------------------------------------------------------------------
H2 — ALTA — logout frontend puede no revocar la sesión por usar sendBeacon sin CSRF
--------------------------------------------------------------------------------

Evidencia — código actual:
Archivo: apps/backend/api/src/app/auth/auth.controller.ts
Líneas: 264-272
    @Post('logout')
    @HttpCode(HttpStatus.OK)
    @UseGuards(JwtAuthGuard, CsrfGuard)
    async logout(... ) {
      await this.authService.logout(user.id);
      this.cookieService.clearAuthCookies(res);

Archivo: apps/core/client-web/src/app/core/services/auth.ts
Líneas: 321-355
    logout(notifyBackend = true): void {
      this._currentUser.set(null);
      this._authStatus.set(AuthStatus.unauthenticated);
      ...
      if (notifyBackend) {
        const url = `${this.apiUrl}/logout`;
        const beaconSent = typeof navigator !== 'undefined' && navigator.sendBeacon
          ? navigator.sendBeacon(url, new Blob([JSON.stringify({})], { type: 'application/json' }))
          : false;

        if (!beaconSent) {
          this.http.post(url, {}, {
            withCredentials: true,
            context: new HttpContext().set(IS_PUBLIC_API, true),
          })...
        }
      }
    }

Archivo: apps/core/client-web/src/app/core/interceptors/auth.interceptor.ts
Líneas: 30-36
    const xsrfToken = tokenExtractor.getToken();
    if (xsrfToken && !['GET', 'HEAD', 'OPTIONS'].includes(req.method)) {
      authReq = authReq.clone({
        headers: authReq.headers.set('X-XSRF-TOKEN', xsrfToken),
      });
    }

Explicación:
sendBeacon no permite añadir el header X-XSRF-TOKEN. El endpoint /auth/logout exige CsrfGuard. Si navigator.sendBeacon devuelve true, el código no ejecuta el fallback HttpClient, aunque el servidor pueda rechazar la petición por CSRF. El frontend ya limpió estado local, pero el refresh token puede permanecer válido en servidor/cookie hasta expiración o hasta otro evento. Esto viola revocación confiable de sesión.

Corrección propuesta:
    // Evitar sendBeacon para logout protegido por CSRF.
    logout(notifyBackend = true): void {
      if (!notifyBackend) { this.clearLocalAuthState(); return; }

      this.http.post(`${this.apiUrl}/logout`, {}, { withCredentials: true })
        .pipe(finalize(() => this.clearLocalAuthState()))
        .subscribe({
          error: () => this.notificationService.showWarning('No se pudo revocar la sesión en servidor.'),
        });
    }

    // Si se requiere logout en unload, crear endpoint dedicado con SameSite+Origin validation y token en body/cookie,
    // o aceptar que sendBeacon no sea mecanismo principal de revocación.

Referencia:
OWASP Session Management Cheat Sheet exige invalidación efectiva en logout; OWASP CSRF Prevention Cheat Sheet exige token no predecible en requests de estado; CWE-613 Insufficient Session Expiration; CWE-352 CSRF.

--------------------------------------------------------------------------------
H3 — ALTA — Edición administrativa permite cambiar email sin verificación y sin invalidar sesiones salvo cambio de rol
--------------------------------------------------------------------------------

Evidencia — código actual:
Archivo: apps/backend/api/src/app/users/dto/update-user.dto.ts
Líneas: 12-14
    @IsOptional()
    @IsEmail()
    email?: string;

Archivo: apps/backend/api/src/app/users/users.controller.ts
Líneas: 183-197
    @Patch(':id')
    @UseGuards(CsrfGuard, TwoFactorVerifiedGuard)
    @HasPermission(PERMISSIONS.USERS_EDIT)
    async update(... @Body() updateUserDto: UpdateUserDto, ...) {
      const updatedUser = await this.usersService.updateUser(id, updateUserDto, user.organizationId);
      return plainToInstance(UserResponseDto, updatedUser, { excludeExtraneousValues: true });
    }

Archivo: apps/backend/api/src/app/users/users.service.ts
Líneas: 130-149
    const { roleId, ...userData } = updateUserDto;
    Object.assign(user, userData);

    if (roleId) {
      ...
      user.roles = [role];
      if (user.security) {
          user.security.tokenVersion = (user.security.tokenVersion || 0) + 1;
      }
      await this.userCacheService.clearUserSession(id);
    } else {
      await this.userCacheService.clearUserSession(id);
    }

Explicación:
El flujo seguro de cambio de email propio sí requiere contraseña actual y token de confirmación en apps/backend/api/src/app/users/users.service.ts:275-333. Sin embargo, el flujo administrativo acepta email en UpdateUserDto y lo aplica directamente. Esto permite cambiar la identidad de login y canal de recuperación sin prueba de control del nuevo correo. Además, tokenVersion sólo incrementa si cambia roleId; cambiar email no invalida access/refresh tokens existentes. Aunque hay permisos y 2FA para el admin, en estándares empresariales el cambio de identificador primario debe tener workflow explícito, auditoría y verificación del nuevo canal o una política administrativa documentada con invalidación inmediata.

Corrección propuesta:
    // apps/backend/api/src/app/users/dto/update-user.dto.ts
    export class UpdateUserDto {
      @IsOptional() @IsString() firstName?: string;
      @IsOptional() @IsString() lastName?: string;
      @IsOptional() @IsUUID() roleId?: string;
      // email eliminado del DTO genérico
    }

    // Crear endpoint dedicado:
    @Post(':id/email-change/request')
    @UseGuards(CsrfGuard, TwoFactorVerifiedGuard)
    @HasPermission(PERMISSIONS.USERS_EDIT)
    async requestManagedEmailChange(...) {
      // generar token al nuevo email, no cambiar user.email todavía
    }

    // Si por política enterprise se permite cambio admin inmediato:
    user.email = dto.email;
    user.isEmailVerified = false;
    user.security.tokenVersion = (user.security.tokenVersion || 0) + 1;
    await this.sessionService.terminateAllSessions(user.id);
    // emitir evento de auditoría y notificar email anterior + nuevo

Referencia:
OWASP ASVS V2 Authentication y V3 Session Management; OWASP Forgot Password Cheat Sheet sobre cambios de email; CWE-620 Unverified Password Change; CWE-287 Improper Authentication.

--------------------------------------------------------------------------------
H4 — ALTA — Flujo frontend para habilitar 2FA no envía currentPassword requerido por backend
--------------------------------------------------------------------------------

Evidencia — código actual:
Archivo: apps/backend/api/src/app/auth/dto/enable-2fa.dto.ts
Líneas: 13-19
    @ApiProperty({ description: 'Current account password for step-up verification' })
    @IsString()
    @IsNotEmpty()
    currentPassword: string;

Archivo: apps/backend/api/src/app/auth/auth.controller.ts
Líneas: 391-399
    @Post('2fa/enable')
    @UseGuards(JwtAuthGuard, CsrfGuard)
    async enableTwoFactor(
      @CurrentUser() user: User,
      @Body() enableTwoFactorDto: EnableTwoFactorDto,
    ) {
      return this.twoFactorAuthService.enableTwoFactor(user, enableTwoFactorDto.token, enableTwoFactorDto.currentPassword);
    }

Archivo: apps/backend/api/src/app/auth/services/two-factor-auth.service.ts
Líneas: 90-99
    if (!freshUser.security.passwordHash) {
      throw new BadRequestException('Password-based step-up is required but this account has no password set.');
    }
    const isPasswordValid = await this.passwordService.verify(freshUser.security.passwordHash, currentPassword);
    if (!isPasswordValid) {
      throw new UnauthorizedException('Current password is incorrect');
    }

Archivo: apps/core/client-web/src/app/core/services/auth.ts
Líneas: 228-230
    enable2fa(token: string): Observable<any> {
        return this.http.post(`${this.apiUrl}/2fa/enable`, { token });
    }

Explicación:
El backend correctamente exige step-up con contraseña para habilitar 2FA, pero el cliente no envía currentPassword. Esto rompe el flujo legítimo de usuarios y crea presión para eliminar la defensa en backend. En seguridad empresarial, la UI debe soportar el control fuerte, no forzar una excepción operacional.

Corrección propuesta:
    // frontend AuthService
    enable2fa(token: string, currentPassword: string): Observable<any> {
      return this.http.post(`${this.apiUrl}/2fa/enable`, { token, currentPassword });
    }

    // UI: solicitar currentPassword en modal seguro antes de llamar enable2fa.
    // Backend: mantener requisito; no relajar a sólo token TOTP.

Referencia:
NIST SP 800-63B step-up/reauthentication para operaciones sensibles; OWASP MFA Cheat Sheet; OWASP ASVS 2.2.2; CWE-306 Missing Authentication for Critical Function.

--------------------------------------------------------------------------------
H5 — MEDIA — DTO/Swagger documenta tokens en cuerpo pese a política cookie-only
--------------------------------------------------------------------------------

Evidencia — código actual:
Archivo: apps/backend/api/src/app/auth/dto/auth-response.dto.ts
Líneas: 3-11
    export class AuthResponseDto {
      @ApiProperty({ required: false })
      accessToken?: string;

      @ApiProperty({ required: false })
      refreshToken?: string;

      @ApiProperty()
      user: any;
    }

Archivo: apps/backend/api/src/app/auth/auth.controller.ts
Líneas: 160-164
    return {
      user: plainToInstance(UserResponseDto, user, { excludeExtraneousValues: true }),
      // 10/10 SECURITY: DO NOT return accessToken in body if using cookies.
      // JS doesn't need it and it increases XSS surface.
    } as any;

Archivo: apps/core/client-web/src/app/core/services/auth.ts
Líneas: 30-36
    interface LoginResponse {
      user: User;
    }

Explicación:
La implementación real evita tokens en el cuerpo, lo cual es correcto. Pero AuthResponseDto/OpenAPI sigue publicando accessToken y refreshToken como campos posibles. En equipos grandes, los contratos generados son fuente de verdad para clientes, SDKs y QA. Esta discrepancia invita a reintroducir tokens en JSON o a que consumidores esperen un patrón inseguro.

Corrección propuesta:
    export class AuthResponseDto {
      @ApiProperty({ type: () => UserResponseDto })
      user: UserResponseDto;
    }

    export class TwoFactorRequiredDto {
      @ApiProperty() require2fa: boolean;
      @ApiProperty() message: string;
    }

Referencia:
OWASP ASVS 1.5 Secure Software Lifecycle; OWASP API Security API8 Security Misconfiguration; CWE-200 Exposure of Sensitive Information.

--------------------------------------------------------------------------------
H6 — MEDIA — /auth/logout revoca todas las sesiones; no hay logout claro de sesión actual
--------------------------------------------------------------------------------

Evidencia — código actual:
Archivo: apps/backend/api/src/app/auth/auth.controller.ts
Líneas: 264-272
    @Post('logout')
    @UseGuards(JwtAuthGuard, CsrfGuard)
    async logout(@CurrentUser() user: User, @Res({ passthrough: true }) res: Response) {
      await this.authService.logout(user.id);
      this.cookieService.clearAuthCookies(res);

Archivo: apps/backend/api/src/app/auth/auth.service.ts
Líneas: 168-171
    async logout(userId: string) {
      // Delegated to SessionService which manages session lifecycle
      await this.sessionService.terminateAllSessions(userId);
      return { message: 'Sesión cerrada exitosamente.' };
    }

Archivo: apps/backend/api/src/app/auth/services/session.service.ts
Líneas: 363-371
    async terminateAllSessions(userId: string) {
      await this.userSecurityRepository.increment({ userId }, 'tokenVersion', 1);
      await this.userCacheService.clearUserSession(userId);
      await this.refreshTokenRepository.update(
        { userId, isRevoked: false },
        { isRevoked: true, revokedAt: new Date() },
      );
    }

Explicación:
Revocar todas las sesiones puede ser útil para “logout all devices”, pero el endpoint se llama simplemente /logout y se invoca desde logout normal de la UI. Esto rompe expectativas de sesión concurrente y elimina todos los dispositivos ante un logout local. El producto también tiene endpoint para listar/revocar sesiones específicas, por lo que falta una separación explícita entre logout current-session y logout-all.

Corrección propuesta:
    // POST /auth/logout: revocar sólo refresh jti actual leído de cookie.
    // POST /auth/logout-all: incrementar tokenVersion y revocar familia completa.
    async logoutCurrentSession(refreshToken: string, userId: string) {
      const payload = verifyRefresh(refreshToken);
      await this.refreshTokenRepository.update({ id: payload.jti, userId }, { isRevoked: true, revokedAt: new Date() });
    }

Referencia:
OWASP Session Management Cheat Sheet recomienda cierre de sesión explícito e invalidación del identificador de sesión correspondiente; CWE-613.

--------------------------------------------------------------------------------
H7 — MEDIA — Reintento automático del refresh token puede ser peligroso si no es idempotente
--------------------------------------------------------------------------------

Evidencia — código actual:
Archivo: apps/core/client-web/src/app/core/interceptors/auth.interceptor.ts
Líneas: 53-72
    return authService.refreshAccessToken().pipe(
      retry({
        count: 3,
        delay: (err, retryCount) => {
          const isIdempotent = [
            'GET',
            'HEAD',
            'PUT',
            'DELETE',
            'OPTIONS',
          ].includes(req.method);

          if (err.status === 0 || (err.status >= 500 && isIdempotent)) {
            return timer(1000 * Math.pow(2, retryCount - 1));
          }
          return throwError(() => err);
        },
      }),

Archivo: apps/backend/api/src/app/auth/services/session.service.ts
Líneas: 121-145
    const lockResult = await this.dataSource.transaction('SERIALIZABLE', async (em) => {
      ...
      if (!rt || rt.isRevoked || rt.expiresAt <= new Date() || rt.tokenHash !== currentTokenHash) {
        return { entity: rt, rotated: false };
      }
      rt.isRevoked = true;
      rt.revokedAt = new Date();
      await em.save(RefreshToken, rt);
      return { entity: rt, rotated: true };
    });

Explicación:
El refresh endpoint rota tokens; por definición tiene side effects. El retry decide por el método original que falló, no por la idempotencia del refresh. Si el servidor rota el token pero la respuesta se pierde (status 0), el retry con el refresh viejo puede activar reuse detection y revocar la familia. El backend tiene defensa estricta, pero el cliente debería tratar refresh como operación no idempotente salvo diseño explícito de “idempotency key” o successor retrieval.

Corrección propuesta:
    // No retry automático de refresh. Si falla por red, mantener estado pending y pedir retry explícito.
    return authService.refreshAccessToken().pipe(
      switchMap(...),
      catchError(...)
    );

    // Alternativa enterprise: refresh idempotente con requestId guardado server-side y ventana corta.

Referencia:
OAuth 2.0 Security BCP refresh token rotation; OWASP ASVS 3.3.3; CWE-613; CWE-362.

--------------------------------------------------------------------------------
H8 — BAJA — Registro acepta teléfono sin formato E.164
--------------------------------------------------------------------------------

Evidencia — código actual:
Archivo: apps/backend/api/src/app/auth/dto/register-user.dto.ts
Líneas: 87-90
    @ApiProperty({ example: '+18090000000', description: 'User Phone', required: false })
    @IsString()
    @IsOptional()
    phone?: string;

Comparación positiva:
Archivo: apps/backend/api/src/app/users/dto/update-profile.dto.ts
Líneas: 15-18
    @IsOptional()
    @IsString()
    @IsE164PhoneNumber({ message: 'Phone number must be in E.164 format (e.g. +18095551234)' })
    phone?: string;

Archivo: apps/backend/api/src/app/auth/auth.controller.ts
Líneas: 441-445
    const e164Regex = /^\+[1-9]\d{6,14}$/;
    if (!e164Regex.test(phoneNumber)) {
        throw new BadRequestException('Phone number must be in E.164 format (e.g. +18091234567)');
    }

Explicación:
Hay validación E.164 en actualización de perfil y envío OTP, pero no en registro. La inconsistencia permite datos de teléfono no normalizados justo antes de flujos de verificación/MFA.

Corrección propuesta:
    @IsOptional()
    @IsString()
    @IsE164PhoneNumber({ message: 'Phone number must be in E.164 format (e.g. +18095551234)' })
    phone?: string;

Referencia:
OWASP Input Validation Cheat Sheet; CWE-20 Improper Input Validation.

--------------------------------------------------------------------------------
H9 — BAJA — Providers HTTP y Service Worker duplicados en frontend
--------------------------------------------------------------------------------

Evidencia — código actual:
Archivo: apps/core/client-web/src/app/app.config.ts
Líneas: 38 y 93-99
    provideHttpClient(),
    ...
    provideHttpClient(withInterceptors([authInterceptor]), withFetch()), provideServiceWorker('ngsw-worker.js', {
            enabled: !isDevMode(),
            registrationStrategy: 'registerWhenStable:30000'
          }), provideServiceWorker('ngsw-worker.js', {
            enabled: !isDevMode(),
            registrationStrategy: 'registerWhenStable:30000'
          }),

Explicación:
No es un exploit directo, pero en autenticación la cadena de interceptores y fetch/XSRF debe ser inequívoca. Providers duplicados aumentan riesgo de comportamiento dependiente de orden y dificultan garantías de que todos los requests pasan por authInterceptor.

Corrección propuesta:
    providers: [
      ...CORE_PROVIDERS_SIN_provideHttpClient,
      provideHttpClient(withInterceptors([authInterceptor]), withFetch()),
      provideServiceWorker('ngsw-worker.js', { enabled: !isDevMode(), registrationStrategy: 'registerWhenStable:30000' }),
    ]

Referencia:
OWASP Secure Design principles: minimize complexity and secure configuration; CWE-710 Improper Adherence to Coding Standards.

================================================================================
6. HOJA DE RUTA DE REMEDIACIÓN PRIORIZADA
================================================================================

FASE 1 — Crítico (antes de cualquier deploy a producción)

1. Corregir /auth/status para usar JwtAuthGuard o verificación RS256 con kid/issuer/audience idéntica al JwtStrategy. Esfuerzo: Medio.
   Evidencia: apps/backend/api/src/app/auth/services/session.service.ts:374-378 y apps/backend/api/src/app/auth/services/token.service.ts:236-244.

2. Reemplazar logout sendBeacon por HttpClient con CSRF o diseñar endpoint dedicado compatible con beacon y protección equivalente. Esfuerzo: Medio.
   Evidencia: apps/core/client-web/src/app/core/services/auth.ts:321-355 y apps/backend/api/src/app/auth/auth.controller.ts:264-272.

3. Eliminar email de UpdateUserDto o crear flujo administrativo dedicado con verificación del nuevo email, auditoría, notificaciones e invalidación de sesiones. Esfuerzo: Alto.
   Evidencia: apps/backend/api/src/app/users/dto/update-user.dto.ts:12-14 y apps/backend/api/src/app/users/users.service.ts:130-149.

4. Actualizar UI/AuthService para enviar currentPassword al habilitar 2FA, manteniendo step-up en backend. Esfuerzo: Bajo/Medio.
   Evidencia: apps/core/client-web/src/app/core/services/auth.ts:228-230 y apps/backend/api/src/app/auth/dto/enable-2fa.dto.ts:13-19.

FASE 2 — Importante (próximo sprint)

5. Separar /auth/logout de /auth/logout-all; logout normal debe revocar sólo la sesión actual si el producto soporta sesiones concurrentes. Esfuerzo: Medio.
   Evidencia: apps/backend/api/src/app/auth/auth.service.ts:168-171 y apps/backend/api/src/app/auth/services/session.service.ts:363-371.

6. Quitar accessToken/refreshToken de AuthResponseDto y de Swagger; publicar DTOs específicos cookie-only. Esfuerzo: Bajo.
   Evidencia: apps/backend/api/src/app/auth/dto/auth-response.dto.ts:3-11.

7. Eliminar retry automático del refresh token o implementar idempotency key server-side para refresh. Esfuerzo: Medio.
   Evidencia: apps/core/client-web/src/app/core/interceptors/auth.interceptor.ts:53-72.

8. Normalizar validación de phone en registro con IsE164PhoneNumber. Esfuerzo: Bajo.
   Evidencia: apps/backend/api/src/app/auth/dto/register-user.dto.ts:87-90.

FASE 3 — Calidad y escalabilidad (deuda técnica planificada)

9. Consolidar provider HTTP y Service Worker en app.config.ts para una única configuración de interceptores, XSRF y fetch. Esfuerzo: Bajo.
   Evidencia: apps/core/client-web/src/app/app.config.ts:38 y 93-99.

10. Revisar contratos OpenAPI completos para que ningún endpoint cookie-only documente bearer tokens en cuerpo. Esfuerzo: Medio.
    Evidencia inicial: apps/backend/api/src/app/auth/dto/auth-response.dto.ts:3-11.

11. Añadir pruebas e2e de sesión: login, hard refresh, /auth/status, refresh rotation con dos requests concurrentes, logout current-session, logout-all, 2FA enable con step-up, cambio de email verificado. Esfuerzo: Alto.
    Evidencia relacionada: múltiples hallazgos H1-H7.

================================================================================
REFERENCIAS DE INDUSTRIA CITADAS
================================================================================

- OWASP Application Security Verification Standard (ASVS), especialmente V2 Authentication, V3 Session Management, V4 Access Control.
- OWASP Session Management Cheat Sheet.
- OWASP CSRF Prevention Cheat Sheet.
- OWASP JWT Cheat Sheet.
- OWASP Forgot Password Cheat Sheet.
- OWASP Logging Cheat Sheet.
- OWASP Input Validation Cheat Sheet.
- OWASP API Security Top 10 2023: API1 Broken Object Level Authorization, API3 Broken Object Property Level Authorization, API8 Security Misconfiguration.
- OAuth 2.0 Security Best Current Practice, refresh token rotation/reuse detection.
- RFC 6265bis: cookie prefixes and cookie attributes.
- RFC 7515: JSON Web Signature and kid header.
- RFC 7519: JSON Web Token issuer/audience validation.
- NIST SP 800-63B: reauthentication / step-up for sensitive operations.
- CWE-20, CWE-200, CWE-203, CWE-287, CWE-306, CWE-347, CWE-352, CWE-362, CWE-532, CWE-613, CWE-620, CWE-640, CWE-710, CWE-915.
