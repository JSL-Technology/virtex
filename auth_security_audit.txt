================================================================================
AUDITORÍA DE SEGURIDAD — AUTENTICACIÓN, AUTORIZACIÓN, USUARIOS Y SESIONES
Proyecto: Virteex ERP (NestJS/Fastify backend + Angular frontend)
Fecha: 2026-06-02
Alcance: EXCLUSIVAMENTE autenticación, autorización, usuarios, roles, tokens y
         seguridad de sesiones (backend y frontend). Módulos de negocio
         (contabilidad, inventario, facturación, etc.) quedan fuera de alcance.
Benchmark de calidad: Auth0 / GitHub / Stripe.
Metodología: Lectura directa del código del repositorio. Cada hallazgo incluye
             archivo, línea y fragmento real. No se reportan problemas no
             observados directamente en el código.
================================================================================


╔══════════════════════════════════════════════════════════════════════════╗
║ 1. RESUMEN EJECUTIVO                                                       ║
╚══════════════════════════════════════════════════════════════════════════╝

El módulo de autenticación de Virteex es maduro, está bien estructurado y refleja
varias rondas previas de endurecimiento (se ven marcas H1..H18 de auditorías
anteriores ya corregidas). Las primitivas centrales son sólidas y, en varios
puntos, de nivel empresarial: hashing Argon2id, cookies httpOnly (cero tokens en
localStorage en el frontend), rotación de refresh tokens con detección de reuso e
invalidación de familia, CSRF de doble-envío firmado con HMAC, Helmet con HSTS/CSP,
rate limiting respaldado por Redis, validación de entorno con fail-fast (Joi),
aislamiento multi-tenant en consultas de usuarios, y un guard que evita escalación
de privilegios al CREAR/EDITAR roles.

Sin embargo, NO está listo para un despliegue a escala empresarial tal cual: existe
una vía REAL de escalación de privilegios en la ASIGNACIÓN de roles a usuarios
(`UsersService.updateUser` no valida que el actor pueda otorgar el rol que asigna),
que permite a un usuario con `users:edit` —sin ser administrador— auto-asignarse el
rol ADMINISTRATOR (que posee `*`). A esto se suman hallazgos de severidad media:
vinculación de cuentas OAuth sin verificar el claim `email_verified` del proveedor,
token de invitación almacenado en texto plano (a diferencia del de reseteo, que sí
se hashea), TLS de base de datos con verificación de certificado deshabilitada, una
política ABAC (`IsOrganizationOwnerPolicy`) cuya lógica es incorrecta/inerte, y un
desajuste entre el catálogo de permisos y los permisos sembrados en los roles por
defecto.

VEREDICTO: Apto para producción SÓLO tras corregir la Fase 1 (la escalación de
privilegios es bloqueante). Tras ello, el módulo se ubica claramente por encima de
la media de la industria. Puntuación global: 7.5/10.


╔══════════════════════════════════════════════════════════════════════════╗
║ 2. TABLA DE CALIFICACIONES                                                 ║
╚══════════════════════════════════════════════════════════════════════════╝

BACKEND
  Seguridad:          7/10  Primitivas excelentes; arrastrado por escalación de
                            privilegios en asignación de roles (H-01), linking OAuth
                            sin email_verified (M-02), token de invitación en claro
                            (M-03) y TLS DB sin validar certificado (M-04).
  Arquitectura:       8/10  Separación limpia (facade + servicios + guards + policies);
                            puntos débiles: política ABAC inerte (M-05) y algún
                            forwardRef/acoplamiento por dependencias circulares.
  Validación:         8/10  DTOs con whitelist global + forbidNonWhitelisted; política
                            de contraseña fuerte y compartida; resta el desajuste del
                            catálogo de permisos (M-06) y permiso con typo (L-08).
  Manejo de errores:  8/10  Mensajes genéricos anti-enumeración, fail-secure, filtro
                            global de excepciones de TypeORM.
  Escalabilidad:      8/10  Redis para throttling y caché de sesión, circuit breaker,
                            limpieza por lotes; resta la condición de carrera en la
                            rotación de refresh (L-14).
  Observabilidad:     8/10  Logs estructurados (pino), correlation IDs, eventos de
                            auditoría, hashing de PII y enmascarado de IP; resta un log
                            de OAuth con email/IP en claro (L-10).

FRONTEND
  Seguridad del cliente:    9/10  Tokens SIEMPRE en cookies httpOnly; nada en
                                  localStorage/sessionStorage; estado en signals.
  Manejo tokens/sesión:     9/10  Refresh con cola anti-condición-de-carrera; CSRF
                                  reinyectado tras refresh; withCredentials consistente.
  Protección de rutas:      7/10  Guards correctos pero (por diseño) sólo cosméticos en
                                  cliente; divergencia de wildcards con backend (L-07).
  Consistencia con backend: 7/10  Política de contraseña espejada correctamente; resta
                                  la divergencia de semántica de permisos (L-07, M-06).

  ──────────────────────────────────
  PUNTUACIÓN GLOBAL:    7.5/10
  ──────────────────────────────────
  (Subiría a ~8.5/10 una vez resuelta la Fase 1.)


╔══════════════════════════════════════════════════════════════════════════╗
║ 3. FORTALEZAS — LO QUE ESTÁ BIEN IMPLEMENTADO                              ║
╚══════════════════════════════════════════════════════════════════════════╝

F-1. Cero tokens en almacenamiento accesible por JS (frontend).
     Archivo: apps/core/client-web/src/app/core/services/auth.ts (todo el servicio)
     Toda llamada usa `withCredentials: true` y el estado se guarda como objeto
     `User` en signals; NUNCA se escriben access/refresh tokens en localStorage.
     Ej. login() líneas 142-172, refreshAccessToken() 121-135.
     Por qué es correcto: elimina el vector XSS→robo de token.
     Ref: OWASP "HTML5 Security / Token Storage Cheat Sheet"; OWASP ASVS 3.5.3.

F-2. Hashing Argon2id + ecualización de tiempos + errores genéricos.
     Archivo: apps/backend/api/src/app/auth/services/password.service.ts:13-23
              apps/backend/api/src/app/auth/auth.service.ts:58-76
     `verifyDummy()` ejecuta Argon2 incluso cuando el usuario no existe y siempre
     se lanza `INVALID_CREDENTIALS` genérico + `simulateDelay()`.
     Por qué: evita enumeración por timing y por mensaje.
     Ref: OWASP Authentication Cheat Sheet; CWE-203/CWE-307; NIST SP 800-63B.

F-3. Rotación de refresh tokens con detección de reuso e invalidación de familia.
     Archivo: apps/backend/api/src/app/auth/services/session.service.ts:106-168
     Si se presenta un jti ya revocado → se incrementa `tokenVersion`, se purga la
     caché y se revocan TODOS los refresh del usuario.
     Por qué: patrón de "refresh token rotation" recomendado.
     Ref: OAuth 2.0 Security BCP (RFC 9700) §4.14.2; Auth0 Refresh Token Rotation.

F-4. Defensa por defecto: JwtAuthGuard y ThrottlerGuard globales.
     Archivo: apps/backend/api/src/app/app.module.ts:232-241
     Todo endpoint requiere JWT salvo que se marque `@Public()` explícitamente
     (guards/jwt/jwt.guard.ts:12-23). Rate limit global 20/60s y 5/60s en auth.
     Ref: principio "secure by default"; OWASP API4:2023 (Rate Limiting).

F-5. CSRF de doble-envío FIRMADO (HMAC) con comparación timing-safe.
     Archivo: apps/backend/api/src/app/auth/services/cookie.service.ts:51-92
              apps/backend/api/src/app/auth/guards/csrf.guard.ts:21-44
     Token = nonce.HMAC(secret,nonce); el guard recomputa el HMAC y usa
     `crypto.timingSafeEqual`. Cookies httpOnly + Secure(prod) + SameSite=Lax.
     Ref: OWASP CSRF Cheat Sheet (Signed Double-Submit Cookie).

F-6. Hardening de cabeceras y CORS con allowlist.
     Archivo: apps/backend/api/src/main.ts:38-77
     Helmet con HSTS (1 año, includeSubDomains, preload) en prod, CSP sin
     unsafe-inline en prod, frameAncestors 'none', objectSrc 'none'; CORS por
     lista configurable (no comodín) con credentials.
     Ref: OWASP Secure Headers Project; CWE-1021 (clickjacking).

F-7. Validación de entorno fail-fast para secretos.
     Archivo: apps/backend/api/src/app/app.module.ts:85-104 (Joi),
              apps/backend/api/src/app/auth/auth.config.ts:6-17 (requireSecret)
     JWT_SECRET / JWT_REFRESH_SECRET / CSRF_SECRET / ENCRYPTION_SECRET exigen
     min 32 chars; valores tipo "change_me/default/secret" abortan el arranque.
     Ref: CWE-798 (credenciales embebidas); 12-Factor App.

F-8. Aislamiento multi-tenant en consultas de usuario (anti-IDOR).
     Archivo: apps/backend/api/src/app/users/users.service.ts:218-227 (findOneByOrg),
              users.controller.ts:158-165, 216-226; ParseUUIDPipe en todos los :id.
     Por qué: previene lectura/edición cruzada entre organizaciones.
     Ref: OWASP API1:2023 (BOLA/IDOR); CWE-639.

F-9. Anti-escalación al crear/editar/clonar roles.
     Archivo: apps/backend/api/src/app/roles/roles.service.ts:31-71
     `assertAssignablePermissions` impide asignar a un rol permisos que el actor no
     posee y prohíbe delegar el comodín `*`. DTO valida cada permiso contra el
     catálogo (`@IsIn(ALL_PERMISSIONS)`, create-role.dto.ts:14-18).
     Ref: CWE-269 (Privilege Management). [Ver H-01: este control NO cubre la
     asignación de roles a usuarios.]

F-10. 2FA robusto.
     Archivo: apps/backend/api/src/app/auth/services/two-factor-auth.service.ts
     - Secreto TOTP cifrado AES-256-GCM en reposo (cryptoUtil.encrypt, líneas 39-43).
     - Backup codes hasheados con Argon2 y "burn-on-use" (159-178).
     - Step-up con contraseña para activar 2FA (98-108).
     Ref: NIST SP 800-63B §5.1.5; OWASP MFA Cheat Sheet.

F-11. WebAuthn correcto.
     Archivo: apps/backend/api/src/app/auth/services/webauthn.service.ts
     userVerification 'required', challenge de un solo uso con TTL (del tras uso,
     líneas 90/157), verificación de counter (clon detection, 151-156).
     Ref: W3C WebAuthn; FIDO2.

F-12. Aislamiento de campos sensibles + DTO de salida con whitelist.
     Archivo: apps/backend/api/src/app/users/entities/user-security.entity.ts
              apps/backend/api/src/app/auth/dto/user-response.dto.ts (@Expose)
     passwordHash, twoFactorSecret, backupCodes, tokens viven en `UserSecurity`
     (entidad separada, no eager) y nunca se exponen; las respuestas usan
     `plainToInstance(..., { excludeExtraneousValues: true })`.
     Ref: OWASP API3:2023 (Excessive Data Exposure); CWE-213.

F-13. Hardening de privacidad en logs: hash de email y enmascarado/cifrado de IP.
     Archivo: token.service.ts:204-235; session.service.ts:210-224
     IP mostrada enmascarada; IP cifrada (AES-GCM, columna select:false) sólo para
     forense; emails hasheados antes de loguear (H9/H14).
     Ref: GDPR minimización; OWASP Logging Cheat Sheet.


╔══════════════════════════════════════════════════════════════════════════╗
║ 4. TABLA RESUMEN DE HALLAZGOS NEGATIVOS                                    ║
╚══════════════════════════════════════════════════════════════════════════╝

SEV | ID   | Tipo                  | Capa     | Archivo                                   | Línea     | Resumen
----|------|-----------------------|----------|-------------------------------------------|-----------|--------------------------------------------------------------
ALTA| H-01 | Escalación privilegios| Backend  | users/users.service.ts                    | 146-181   | updateUser permite asignar cualquier rol del org (incl. ADMINISTRATOR/`*`) sin validar al actor
MED | M-02 | Auth/Account takeover | Backend  | auth/services/social-auth.service.ts      | 27-37     | Linking OAuth por email sin verificar claim email_verified del proveedor
MED | M-03 | Mal almacenamiento    | Backend  | users/users.service.ts + password-recov.  | 323-335 / 88-113 | Token de invitación en texto plano (el de reseteo sí se hashea)
MED | M-04 | Config TLS            | Backend  | app.module.ts                             | 151-153   | Conexión a Postgres con rejectUnauthorized:false (MITM de credenciales)
MED | M-05 | Autorización (ABAC)   | Backend  | auth/policies/is-organization-owner.policy| 10-30     | Política inerte/incorrecta: compara userId param vs orgId y rol 'ADMIN' inexistente
MED | M-06 | Inconsistencia RBAC   | Backend  | config/roles.config.ts vs shared/perms.ts | toda      | Roles por defecto siembran permisos que NO existen en el catálogo
BAJA| L-07 | Inconsistencia        | Ambos    | guards/permissions.guard.ts vs util-auth  | 46-51     | Wildcards de permiso difieren: guard exacto vs util con prefijo `*`
BAJA| L-08 | Inconsistencia/typo   | Backend  | users/users.controller.ts                 | 238       | @HasPermission('users.edit') (punto) ≠ catálogo 'users:edit' (dos puntos)
BAJA| L-09 | Falta step-up         | Backend  | auth/auth.controller.ts                   | 333-346   | Impersonate sin TwoFactorVerifiedGuard (otras ops sensibles sí lo exigen)
BAJA| L-10 | PII en logs           | Backend  | auth/services/social-auth.service.ts      | 45-52     | Audit de login social loguea email e IP en claro
BAJA| L-11 | Token reusable        | Backend  | auth/auth.service.ts                       | 96-102    | tempToken 2FA no es de un solo uso (replay dentro de 5 min)
BAJA| L-12 | Config muerta         | Backend  | auth/services/password.service.ts          | 13-15     | Parámetros ARGON2_* configurables pero ignorados (usa defaults)
BAJA| L-13 | Cripto                | Backend  | shared/utils/crypto.util.ts                | 19        | Salt estático 'salt'; 3 derivaciones de clave distintas para ENCRYPTION_SECRET
BAJA| L-14 | Race condition        | Backend  | auth/services/session.service.ts           | 108-168   | Rotación de refresh sin bloqueo transaccional → falsos positivos de reuso
INFO| I-15 | Bug menor             | Backend  | security-analysis.service.ts/session.svc   | 115/180   | parseUserAgent no devuelve deviceType → siempre undefined
INFO| I-16 | Diseño                | Backend  | auth/services/registration.service.ts      | 162-172   | Registro deja al usuario ACTIVE sin verificación de email
INFO| I-17 | Enumeración leve      | Backend  | auth/services/registration.service.ts      | 100-111   | Registro responde 409 vs 201 según exista el email (mensaje genérico)


╔══════════════════════════════════════════════════════════════════════════╗
║ 5. HALLAZGOS DETALLADOS                                                    ║
╚══════════════════════════════════════════════════════════════════════════╝

────────────────────────────────────────────────────────────────────────────
[H-01] ALTA — Escalación de privilegios vía asignación de roles a usuarios
────────────────────────────────────────────────────────────────────────────
Capa: Backend
Archivos:
  - apps/backend/api/src/app/users/users.controller.ts:167-182
  - apps/backend/api/src/app/users/users.service.ts:146-181
  - apps/backend/api/src/app/users/dto/update-user.dto.ts:15-18

EVIDENCIA (controller):
  167  @Patch(':id')
  168  @UseGuards(CsrfGuard, TwoFactorVerifiedGuard)
  169  @HasPermission(PERMISSIONS.USERS_EDIT)         // sólo exige 'users:edit'
  ...
  176    const updatedUser = await this.usersService.updateUser(id, updateUserDto, user.organizationId);

EVIDENCIA (service):
  161   const { roleId, ...userData } = updateUserDto;
  165   if (roleId) {
  166     const role = await this.rolesService.findOne(roleId, organizationId);  // sólo valida que el rol exista en el org
  170     user.roles = [role];                                                   // <-- asigna SIN validar al actor
  ...

EVIDENCIA (DTO):
  15  @IsOptional() @IsUUID() roleId?: string;

EXPLICACIÓN:
`assertAssignablePermissions` (roles.service.ts:31-45) protege la CREACIÓN/EDICIÓN
de roles, pero NO la ASIGNACIÓN de un rol existente a un usuario. Todo organización
tiene sembrado el rol ADMINISTRATOR con permisos `['*']` (config/roles.config.ts:4-8).
Un actor con el permiso `users:edit` (pero sin `*`) —un rol "Manager" perfectamente
legítimo que un admin podría crear— puede ejecutar:
   PATCH /users/{suPropioId}  { "roleId": "<id-del-rol-ADMINISTRATOR>" }
y obtener el comodín `*`, comprometiendo toda la organización. Es una escalación
vertical de privilegios real (no requiere ser admin previamente).

CORRECCIÓN (validar que el actor pueda otorgar ese rol):
  // users.service.ts — al inicio de updateUser, recibir el actor
  async updateUser(id, dto, organizationId, actor: AuthenticatedUser) {
    ...
    if (roleId) {
      const role = await this.rolesService.findOne(roleId, organizationId);
      if (!role) throw new NotFoundException(...);
      // 1) El actor sólo puede asignar roles cuyos permisos él ya posee:
      this.rolesService.assertCanAssignRole(actor, role); // reutilizar assertAssignablePermissions(actor, role.permissions)
      // 2) (Defensa en profundidad) impedir delegar '*' salvo super-admin:
      if (role.permissions.includes('*') && !actor.permissions.includes('*'))
        throw new ForbiddenException('No puedes asignar un rol con privilegios totales.');
      user.roles = [role];
      ...
    }
  }
Además, exponer `assertAssignablePermissions` como método público de RolesService y
pasar `@CurrentUser()` desde el controller (hoy `updateUser` no recibe el actor).

REFERENCIA: OWASP API3:2023 (Broken Object Property Level Authorization),
CWE-269 (Improper Privilege Management), CWE-266 (Incorrect Privilege Assignment).


────────────────────────────────────────────────────────────────────────────
[M-02] MEDIA — Vinculación de cuentas OAuth sin verificar email_verified
────────────────────────────────────────────────────────────────────────────
Capa: Backend
Archivo: apps/backend/api/src/app/auth/services/social-auth.service.ts:23-56

EVIDENCIA:
  25   const user = await this.usersService.findUserForAuth(socialUser.email);
  27   if (user) {
  28     if (user.authProvider !== socialUser.provider || user.authProviderId !== socialUser.providerId) {
  29       await this.usersService.update(user.id, {
  30         authProvider: socialUser.provider,
  31         authProviderId: socialUser.providerId,   // <-- se (re)vincula automáticamente por coincidencia de email
  ...

EXPLICACIÓN:
Cuando llega un login social cuyo email coincide con una cuenta existente, se vincula
y se SOBRESCRIBE `authProvider`/`authProviderId` con lo que entregue el IdP, sin
comprobar el claim `email_verified` del proveedor. Con Google/Microsoft sobre dominios
gestionados suele venir verificado, pero con proveedores OIDC/Okta personalizados o
dominios no verificados, un atacante que controle un IdP capaz de afirmar un email
ajeno podría apropiarse de la cuenta local (pre-account takeover). También permite que
un segundo proveedor "secuestre" el providerId previamente vinculado.

CORRECCIÓN:
  - Verificar el claim de email del IdP antes de vincular:
      if (!socialUser.emailVerified) throw new UnauthorizedException('Email no verificado por el proveedor.');
    (propagar `email_verified` desde cada estrategia: google/microsoft/okta.strategy.ts).
  - No re-vincular automáticamente si ya existe una contraseña local: requerir un paso
    de "vinculación de cuenta" autenticado (login local o verificación por correo).
REFERENCIA: OWASP "Authentication Cheat Sheet" (account linking), CWE-287,
incidente clásico "Pre-account hijacking via OAuth" (Microsoft/Okta research, 2022).


────────────────────────────────────────────────────────────────────────────
[M-03] MEDIA — Token de invitación almacenado en texto plano
────────────────────────────────────────────────────────────────────────────
Capa: Backend
Archivos:
  - apps/backend/api/src/app/users/users.service.ts:323-335
  - apps/backend/api/src/app/auth/services/password-recovery.service.ts:87-128

EVIDENCIA (generación, en claro):
  323  const invitationToken = crypto.randomBytes(32).toString('hex');
  ...
  334  invitationToken: invitationToken,          // se persiste tal cual

EVIDENCIA (consulta directa por el valor crudo):
  106  const user = await this.userRepository.findOne({
  107    where: { invitationToken: token, status: UserStatus.PENDING, invitationTokenExpires: MoreThan(new Date()) },

CONTRASTE — el token de reseteo SÍ se hashea (buena práctica que aquí falta):
  password-recovery.service.ts:38-43
  38  const rawToken = crypto.randomBytes(32).toString('base64url');
  39  const tokenHash = crypto.createHash('sha256').update(rawToken).digest('hex');
  43  user.security.passwordResetToken = tokenHash;   // se guarda el HASH

EXPLICACIÓN:
El token de invitación permite fijar contraseña y ACTIVAR la cuenta
(setPasswordFromInvitation). Guardarlo en claro es inconsistente con el de reseteo y
expone toma de control de cuentas PENDING ante cualquier lectura de la BD (backup
filtrado, SQLi en otro módulo, acceso de DBA). La entropía (32 bytes) evita fuerza
bruta, pero no la exposición en reposo.

CORRECCIÓN: almacenar SHA-256(token) y buscar por el hash, igual que el reset:
  const raw = crypto.randomBytes(32).toString('base64url');
  user.invitationToken = crypto.createHash('sha256').update(raw).digest('hex');
  // enviar `raw` por correo; en getInvitationDetails/setPassword buscar por hash.
REFERENCIA: CWE-522 (Insufficiently Protected Credentials), OWASP "Forgot Password
Cheat Sheet" (los tokens de un solo uso deben guardarse hasheados).


────────────────────────────────────────────────────────────────────────────
[M-04] MEDIA — TLS de base de datos con verificación de certificado deshabilitada
────────────────────────────────────────────────────────────────────────────
Capa: Backend (infra que sostiene la auth)
Archivo: apps/backend/api/src/app/app.module.ts:151-153

EVIDENCIA:
  151  ssl: config.get<boolean>('DB_SSL', false)
  152    ? { rejectUnauthorized: false }
  153    : false,

EXPLICACIÓN:
Con `rejectUnauthorized:false`, aun "usando TLS" no se valida el certificado del
servidor Postgres → un atacante en la red puede hacer MITM y leer/alterar todo el
tráfico, incluyendo password hashes, refresh tokens (jti) y secretos 2FA cifrados.
Impacta directamente la confidencialidad/integridad de los datos de autenticación.

CORRECCIÓN:
  ssl: config.get('DB_SSL') ? { rejectUnauthorized: true, ca: fs.readFileSync(config.get('DB_SSL_CA')) } : false
REFERENCIA: CWE-295 (Improper Certificate Validation); OWASP TLS Cheat Sheet.


────────────────────────────────────────────────────────────────────────────
[M-05] MEDIA — Política ABAC IsOrganizationOwnerPolicy incorrecta/inerte
────────────────────────────────────────────────────────────────────────────
Capa: Backend
Archivos:
  - apps/backend/api/src/app/auth/policies/is-organization-owner.policy.ts:10-30
  - uso: apps/backend/api/src/app/users/users.controller.ts:184-191 (DELETE /users/:id)

EVIDENCIA:
  15   const resourceId = request.params?.id;          // en DELETE /users/:id ESTO ES UN userId
  19   if (user.roles?.some((r) => r.name === 'ADMIN')) return true;   // el rol real es 'ADMINISTRATOR'
  25   if (resourceId && user.organization?.id === resourceId) return true;  // compara userId vs orgId → nunca true
  29   return false;

EXPLICACIÓN:
1) Compara el `:id` de la ruta (un UUID de USUARIO) contra `user.organization.id`
   (UUID de ORGANIZACIÓN): nunca coincide.
2) El nombre de rol comprobado es `'ADMIN'`, pero el rol real es
   `RoleEnum.ADMINISTRATOR` ('ADMINISTRATOR'): nunca coincide.
En consecuencia la política devuelve SIEMPRE false. La única razón por la que DELETE
/users/:id funciona para administradores es que `PermissionsGuard` cortocircuita ANTES
con el comodín `*` (permissions.guard.ts:42-44), de modo que las políticas ABAC ni
siquiera se evalúan para super-admins. Resultado: lógica de seguridad muerta que da
falsa sensación de control de propiedad, y que jamás restringirá realmente a un actor
no-comodín con `users:delete`.

CORRECCIÓN:
  - Corregir el nombre de rol ('ADMINISTRATOR') o, mejor, basarse en permisos.
  - Cargar el recurso real y comparar su `organizationId` con el del actor:
      const target = await this.usersRepo.findOne({ where:{ id: resourceId }, select:['organizationId'] });
      return !!target && target.organizationId === user.organizationId;
  - Reconsiderar que `*` saltee TODAS las policies: para acciones críticas, las
    policies deberían evaluarse incluso para super-admins (o documentarlo explícitamente).
REFERENCIA: CWE-863 (Incorrect Authorization), CWE-561 (Dead Code en ruta de seguridad).


────────────────────────────────────────────────────────────────────────────
[M-06] MEDIA — Desajuste entre catálogo de permisos y roles por defecto
────────────────────────────────────────────────────────────────────────────
Capa: Backend
Archivos:
  - apps/backend/api/src/app/config/roles.config.ts:16-49
  - apps/backend/api/src/app/shared/permissions.ts:3-105

EVIDENCIA (permisos sembrados que NO existen en el catálogo):
  roles.config.ts:
    43  'journal-entries:create',  'journal-entries:view',     // catálogo usa 'journal_entries:create' (guion bajo)
    44  'chart-of-accounts:view',  'chart-of-accounts:edit',   // catálogo usa 'coa:view' / 'coa:edit'
    38  'reports:view',                                         // catálogo usa 'reports:view_sales' / 'reports:view_financial'
    24  'sales:create',  21 'dashboard:view',  36 'accounting:view', 40 'suppliers:view'  // inexistentes en PERMISSIONS

EXPLICACIÓN:
Los guards comprueban igualdad exacta contra los strings del catálogo
(permissions.guard.ts:49 `user.permissions.includes(requirement)`). Como los roles
ACCOUNTANT/SELLER siembran strings que NO coinciden con `PERMISSIONS`, esos permisos
son "fantasma": no habilitan los endpoints que pretenden y, si algún día se usaran en
un `@HasPermission`, fallarían. Falla en modo seguro (deniega), pero rompe el modelo
RBAC y delata la ausencia de una única fuente de verdad. (Esto es exactamente lo que
la auditoría busca: "permisos definidos en un lugar pero ignorados en otro" y "lógica
de roles contradictoria entre módulos".)

CORRECCIÓN:
  - Tipar DEFAULT_ROLES con `Permission[]` y construirlos desde la constante PERMISSIONS:
      permissions: [PERMISSIONS.INVOICES_VIEW, PERMISSIONS.CHART_OF_ACCOUNTS_VIEW, ...]
    Así el compilador rechaza cualquier string fuera del catálogo.
REFERENCIA: CWE-1190/CWE-863 (autorización inconsistente); principio "single source of truth".


────────────────────────────────────────────────────────────────────────────
[L-07] BAJA — Semántica de wildcards divergente entre guard backend y util compartida
────────────────────────────────────────────────────────────────────────────
Capa: Backend + Frontend (lib compartida)
Archivos:
  - apps/backend/api/src/app/auth/guards/permissions/permissions.guard.ts:46-51 (sólo igualdad exacta + '*')
  - libs/shared/util-auth/src/lib/permissions.util.ts:15-28 (soporta prefijo 'x:*')

EVIDENCIA:
  permissions.guard.ts:49   if (!user.permissions.includes(requirement)) throw ...   // NO entiende 'users:*'
  permissions.util.ts:22    if (userPerm.endsWith('*')) { ... req.startsWith(prefix) } // SÍ entiende 'users:*'

EXPLICACIÓN:
El frontend (permissionsGuard → AuthService.hasPermissions → util) y el backend en
impersonation.service.ts (que también usa `hasPermission`) interpretan wildcards de
prefijo, pero el `PermissionsGuard` del backend no. Un permiso como `users:*` daría
acceso en UI pero 403 en API, o viceversa según el camino. Hoy los roles por defecto
no usan prefijos (sólo `*`), por lo que el impacto práctico es bajo, pero es una
inconsistencia latente que puede causar autorizaciones contradictorias.

CORRECCIÓN: usar la MISMA función `hasPermission` en `PermissionsGuard`:
  if (!hasPermission(user.permissions, [requirement])) throw new ForbiddenException(...);
REFERENCIA: consistencia de control de acceso; CWE-863.


────────────────────────────────────────────────────────────────────────────
[L-08] BAJA — Permiso con typo en adminChangeEmail ('users.edit' vs 'users:edit')
────────────────────────────────────────────────────────────────────────────
Capa: Backend
Archivo: apps/backend/api/src/app/users/users.controller.ts:235-247

EVIDENCIA:
  238  @HasPermission('users.edit')   // PUNTO; el catálogo define 'users:edit' (dos puntos)

EXPLICACIÓN:
`'users.edit'` no existe en `PERMISSIONS`. Para usuarios no-comodín la comprobación
exacta nunca pasa (se deniega), mientras que el endpoint hermano @Patch(':id') usa
`PERMISSIONS.USERS_EDIT`. Funciona sólo para admins por el bypass de `*`. Falla en
modo seguro, pero es inconsistente y confuso.

CORRECCIÓN: `@HasPermission(PERMISSIONS.USERS_EDIT)` y usar siempre la constante
(evita strings mágicos).
REFERENCIA: CWE-655 (Improper Initialization of Security Config) / consistencia.


────────────────────────────────────────────────────────────────────────────
[L-09] BAJA — Impersonation sin step-up 2FA
────────────────────────────────────────────────────────────────────────────
Capa: Backend
Archivo: apps/backend/api/src/app/auth/auth.controller.ts:333-346

EVIDENCIA:
  333  @Post('impersonate')
  334  @UseGuards(JwtAuthGuard, CsrfGuard)        // sin TwoFactorVerifiedGuard

EXPLICACIÓN:
Suplantar a otro usuario es de las acciones más sensibles, pero —a diferencia de
revocar sesiones, deshabilitar 2FA, editar/eliminar usuarios o registrar passkeys,
que sí exigen `TwoFactorVerifiedGuard`— aquí no hay re-autenticación de paso elevado.
Si la sesión del admin es secuestrada, el atacante impersona sin fricción adicional.

CORRECCIÓN: añadir `TwoFactorVerifiedGuard` (y registrar evento de auditoría, que ya
existe) a `impersonate`.
REFERENCIA: OWASP "Sensitive operations require re-authentication"; NIST SP 800-63B
(AAL step-up).


────────────────────────────────────────────────────────────────────────────
[L-10] BAJA — PII en claro en auditoría de login social
────────────────────────────────────────────────────────────────────────────
Capa: Backend
Archivo: apps/backend/api/src/app/auth/services/social-auth.service.ts:45-52

EVIDENCIA:
  50   { email: user.email, provider: socialUser.provider, ipAddress, userAgent },

EXPLICACIÓN:
Inconsistente con el resto del código (que hashea email y enmascara IP — ver
mfa-orchestrator.service.ts:182-205 y session.service.ts:210-224). Filtra PII en el
trail de auditoría.

CORRECCIÓN: hashear email e IP igual que en `complete2faLogin`:
  emailHash: sha256(email).slice(0,12), ipHash/ipMasked, uaHash.
REFERENCIA: GDPR minimización; OWASP Logging Cheat Sheet; CWE-532.


────────────────────────────────────────────────────────────────────────────
[L-11] BAJA — tempToken de 2FA reutilizable dentro de su ventana
────────────────────────────────────────────────────────────────────────────
Capa: Backend
Archivo: apps/backend/api/src/app/auth/auth.service.ts:91-113 (emisión),
         auth.controller.ts:434-457 (verify-2fa)

EVIDENCIA:
  96   const tempToken = this.jwtService.sign({ id, type:'2fa_pending', tokenVersion }, { expiresIn:`${expirationSeconds}s`, secret: JWT_2FA_TEMP_SECRET });

EXPLICACIÓN:
El `tempToken` es un JWT puro: no se invalida tras un intento, por lo que puede
reintentarse mientras no expire (≈5 min). Mitigado por el throttle de verify-2fa
(5/60s), pero permite combinar intentos. Lo ideal es un jti de un solo uso.

CORRECCIÓN: persistir un jti del tempToken (Redis con TTL) y eliminarlo en el primer
`verify-2fa` (éxito o fallo definitivo); o emitir el reto 2FA como registro en BD de
un solo uso.
REFERENCIA: OWASP MFA Cheat Sheet; CWE-294 (Authentication Bypass by Capture-replay).


────────────────────────────────────────────────────────────────────────────
[L-12] BAJA — Parámetros de tuning Argon2 configurables pero no aplicados
────────────────────────────────────────────────────────────────────────────
Capa: Backend
Archivo: apps/backend/api/src/app/auth/services/password.service.ts:13-15
         (config no usada: auth.config.ts:64-68 ARGON2_MEMORY_COST/TIME_COST/PARALLELISM)

EVIDENCIA:
  13   async hash(plain: string): Promise<string> { return argon2.hash(plain); }  // sin opciones

EXPLICACIÓN:
Funcionalmente los defaults de argon2 (argon2id, 64MB, t=3, p=4) coinciden con la
config, así que la seguridad es adecuada; pero la configuración es "muerta" y
engañosa: cambiarla no surte efecto. Riesgo de falsa expectativa operativa.

CORRECCIÓN:
  argon2.hash(plain, { type: argon2.argon2id, memoryCost: AuthConfig.ARGON2_MEMORY_COST,
                       timeCost: AuthConfig.ARGON2_TIME_COST, parallelism: AuthConfig.ARGON2_PARALLELISM });
REFERENCIA: OWASP Password Storage Cheat Sheet (parámetros Argon2id explícitos).


────────────────────────────────────────────────────────────────────────────
[L-13] BAJA — Salt estático y múltiples derivaciones de clave para ENCRYPTION_SECRET
────────────────────────────────────────────────────────────────────────────
Capa: Backend
Archivos:
  - shared/utils/crypto.util.ts:19  → scryptSync(secret, 'salt', 32)   (salt literal)
  - auth/services/token.service.ts:228 → createHash('sha256').update(secret) (otra derivación)
  - auth/services/session.service.ts:78 → scryptSync(secret, AUTH_SALT, 32) (otra más)

EXPLICACIÓN:
Tres esquemas distintos de derivación de clave a partir del mismo `ENCRYPTION_SECRET`.
Cada uno es internamente consistente (cifra/descifra con la misma clave), pero el salt
literal `'salt'` y la divergencia dificultan rotación de claves y revisión. Con un
secreto fuerte el riesgo es bajo.

CORRECCIÓN: centralizar el cifrado en `CryptoUtil`, usar `AUTH_SALT` (no literal) y una
única derivación; planificar versionado/rotación de clave.
REFERENCIA: CWE-760 (Predictable Salt); NIST SP 800-132.


────────────────────────────────────────────────────────────────────────────
[L-14] BAJA — Rotación de refresh sin bloqueo: falsos positivos de reuso
────────────────────────────────────────────────────────────────────────────
Capa: Backend
Archivo: apps/backend/api/src/app/auth/services/session.service.ts:108-168

EXPLICACIÓN:
Entre la lectura `findOne({where:{id:jti}})` y el `save({isRevoked:true})` no hay
transacción/lock. Dos refresh concurrentes legítimos (común en SPAs con varias
pestañas) pueden ambos ver `isRevoked=false` y, en el segundo, dispararse la
detección de reuso → invalidación de TODA la familia y logout global del usuario
legítimo (denegación de servicio para el propio usuario). El grace period
(`AuthConfig.REFRESH_GRACE_PERIOD`) existe pero no se aplica aquí.

CORRECCIÓN: hacer la rotación atómica:
  - UPDATE ... SET is_revoked=true WHERE id=:jti AND is_revoked=false RETURNING ...;
    si affected=0 → ya fue rotado: aplicar grace period (aceptar si revokedAt < now-grace
    y el sucesor pertenece a la misma familia) antes de declarar reuso.
REFERENCIA: RFC 9700 §4.14.2 (refresh rotation con tolerancia a concurrencia); CWE-362.


────────────────────────────────────────────────────────────────────────────
[I-15] INFO — parseUserAgent no provee deviceType
────────────────────────────────────────────────────────────────────────────
Archivo: security-analysis.service.ts:115-129 (devuelve sólo {browser, os})
         session.service.ts:180-182 usa `parsedUA.deviceType` → undefined
Impacto: la columna device_type del refresh token queda siempre nula; no afecta
seguridad (la comparación de dispositivo usa browser+os). Corregir el parser o
eliminar el campo.


────────────────────────────────────────────────────────────────────────────
[I-16] INFO — Registro deja la cuenta ACTIVE sin verificación de email
────────────────────────────────────────────────────────────────────────────
Archivo: auth/services/registration.service.ts:162-172 (status: UserStatus.ACTIVE)
Impacto: cualquiera puede registrarse con un email que no controla y operar de
inmediato. Para estándar empresarial, considerar un flujo de verificación de correo
(estado PENDING → verificación → ACTIVE). Existe infraestructura de OTP por email
(mfa-orchestrator.sendEmailOtp) reutilizable.
Ref: OWASP "Registration" / NIST IAL.


────────────────────────────────────────────────────────────────────────────
[I-17] INFO — Enumeración leve de cuentas en /register
────────────────────────────────────────────────────────────────────────────
Archivo: auth/services/registration.service.ts:100-111
Impacto: email existente → 409; inexistente → 201. El mensaje es genérico y se envía
un "duplicate registration email", pero el código de estado difiere. Bajo impacto
(registrar crea organización; no es un oráculo barato). Aceptable como trade-off, se
documenta por completitud.
Ref: CWE-204 (Response Discrepancy Information Exposure).


╔══════════════════════════════════════════════════════════════════════════╗
║ 6. HOJA DE RUTA DE REMEDIACIÓN PRIORIZADA                                  ║
╚══════════════════════════════════════════════════════════════════════════╝

────────────────────────────────────────────────────────────────────────────
FASE 1 — CRÍTICO (antes de CUALQUIER deploy a producción)
────────────────────────────────────────────────────────────────────────────
  [H-01] Validar asignación de roles a usuarios (anti escalación).      Esfuerzo: MEDIO
         → updateUser debe recibir el actor y aplicar assertAssignablePermissions
           sobre los permisos del rol asignado + bloquear delegación de '*'.
  [M-04] DB TLS con rejectUnauthorized:true + CA.                       Esfuerzo: BAJO
         (config/infra; impacto alto sobre confidencialidad de credenciales)

────────────────────────────────────────────────────────────────────────────
FASE 2 — IMPORTANTE (próximo sprint)
────────────────────────────────────────────────────────────────────────────
  [M-02] Verificar email_verified del IdP y endurecer el linking OAuth. Esfuerzo: MEDIO
  [M-03] Hashear el token de invitación (igual que el de reseteo).      Esfuerzo: BAJO
  [M-05] Corregir/rediseñar IsOrganizationOwnerPolicy (o eliminarla).   Esfuerzo: BAJO
  [M-06] Tipar DEFAULT_ROLES con PERMISSIONS (single source of truth).  Esfuerzo: BAJO
  [L-09] Exigir step-up 2FA en impersonate.                             Esfuerzo: BAJO
  [L-08] Corregir '@HasPermission(users.edit)' → constante del catálogo.Esfuerzo: BAJO

────────────────────────────────────────────────────────────────────────────
FASE 3 — CALIDAD Y ESCALABILIDAD (deuda técnica planificada)
────────────────────────────────────────────────────────────────────────────
  [L-07] Unificar wildcards: usar hasPermission() en PermissionsGuard.  Esfuerzo: BAJO
  [L-14] Rotación de refresh atómica + grace period anti-concurrencia.  Esfuerzo: MEDIO
  [L-11] tempToken 2FA de un solo uso (jti en Redis).                   Esfuerzo: MEDIO
  [L-10] Hashear PII en auditoría de login social.                      Esfuerzo: BAJO
  [L-13] Centralizar cifrado en CryptoUtil + salt no literal + rotación.Esfuerzo: MEDIO
  [L-12] Aplicar parámetros Argon2 explícitos.                          Esfuerzo: BAJO
  [I-15] Corregir/retirar deviceType.                                   Esfuerzo: BAJO
  [I-16] Evaluar verificación de email en el registro.                  Esfuerzo: MEDIO
  [I-17] Homogeneizar respuesta de /register (opcional).               Esfuerzo: BAJO

================================================================================
FIN DEL INFORME
================================================================================
