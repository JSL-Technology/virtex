{
  "$note": "Which application each namespace is emitted to. A namespace may appear under more than one target: it is still defined ONCE, in base/, and the generator writes the same entry to both catalogues, so the two cannot drift. That is the whole point of the single source — the old arrangement had 57 keys defined twice, 23 of them with different Spanish text, and which one a reader saw depended on lookup order.\n\nOwnership is declared rather than inferred from a usage scan. A scan that misses a reference silently ships a catalogue without the key, and the miss surfaces to a customer; a declaration that is wrong is caught by verify-catalogues.mjs, which checks every referenced key against the catalogue its application actually receives.",

  "client-web": {
    "$why": "The browser renders all user-visible text, including the wording of every domain error: the server now sends a code, a message key and its parameters, never prose. So the namespaces the API raises errors from are emitted here too.",
    "namespaces": [
      "accounting", "accounts_payable", "actions", "analytical_reporting", "app_title", "approvals",
      "apps", "audit", "audit_adjustments", "auth", "batch_processes", "billing", "budgets",
      "chart_of_accounts", "charts", "checkout", "common", "compliance", "consolidation", "contacts",
      "core", "cost_accounting", "currencies", "customer_receipts", "customer_service", "customers",
      "dash", "dashboard", "data_exports", "data_imports", "database", "datasheets", "dialog",
      "dimensions", "documents", "einvoicing", "errors", "extensions", "feedback",
      "financial_reporting", "fiscal", "fixed_assets", "forgot_password", "geo_mismatch",
      "global_search", "hcm", "intercompany", "inventory", "invoices", "journal_entries",
      "localization", "login", "main", "manufacturing", "masters", "modules", "my_work", "nav",
      "notifications", "organizations", "overview", "page_titles", "password_strength", "payment",
      "payroll", "permissions", "pos", "price_lists", "procurement", "projects", "purchasing", "pwa",
      "reconciliation", "register", "remedy", "reports", "reset_password", "roadmap", "roles",
      "sales", "search", "set_password", "settings", "shared", "shell", "sidebar", "status_bar",
      "storage", "suppliers", "tabs", "taxes", "transition", "treasury", "unauthorized", "user",
      "users", "validation", "widgets", "wms", "workflows"
    ]
  },

  "api": {
    "$why": "Only what the server renders to text with no browser in the loop: transactional e-mail, the invoice PDF, the account and journal names written into a new tenant's books, the stored notifications, and the units those messages pluralise. Everything else the API used to phrase, it now names.",
    "namespaces": ["mail", "invoice", "ledger", "time", "saas", "notifications", "invoices", "errors"]
  },

  "pos": {
    "$why": "The till is a separate application with its own bundle, so it gets its own small catalogue rather than the whole product's. It shows its own screens, the shared error wording, and the generic confirmations.",
    "namespaces": ["pos", "errors", "common", "actions", "validation"]
  },

  "desktop": {
    "$why": "The Electron shell only draws a native menu; the rest of the window is the web client, which loads its own catalogue.",
    "namespaces": ["desktop"]
  }
}
