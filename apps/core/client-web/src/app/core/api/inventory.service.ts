/**
 * Backward-compatibility barrel.
 *
 * The canonical source is `features/inventory/data/inventory.service.ts`.
 * Update existing imports to that path when touching a file.
 */
export * from '../../features/inventory/data/inventory.service';
