/**
 * Backward-compatibility barrel.
 *
 * The canonical source is `features/approvals/data/workflows.service.ts`.
 * Update existing imports to that path when touching a file.
 */
export * from '../../features/approvals/data/workflows.service';
