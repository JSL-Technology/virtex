/**
 * Backward-compatibility barrel.
 *
 * The canonical source is `features/settings/data/roles.service.ts`.
 * Update existing imports to that path when touching a file.
 */
export * from '../../features/settings/data/roles.service';
