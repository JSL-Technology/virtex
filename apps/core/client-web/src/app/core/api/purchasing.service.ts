/**
 * Backward-compatibility barrel.
 *
 * The canonical source is `features/purchasing/data/purchasing.service.ts`.
 * Update existing imports to that path when touching a file.
 */
export * from '../../features/purchasing/data/purchasing.service';
