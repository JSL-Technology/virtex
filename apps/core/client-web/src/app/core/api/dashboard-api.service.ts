/**
 * Backward-compatibility barrel.
 *
 * The canonical source is `features/dashboard/data/dashboard-api.service.ts`.
 * Update existing imports to that path when touching a file.
 */
export * from '../../features/dashboard/data/dashboard-api.service';
