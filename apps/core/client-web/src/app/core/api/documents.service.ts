/**
 * Backward-compatibility barrel.
 *
 * The canonical source is `features/documents/data/documents.service.ts`.
 * Update existing imports to that path when touching a file.
 */
export * from '../../features/documents/data/documents.service';
