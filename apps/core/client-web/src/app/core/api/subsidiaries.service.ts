/**
 * Backward-compatibility barrel.
 *
 * The canonical source is `features/settings/data/subsidiaries.service.ts`.
 * Update existing imports to that path when touching a file.
 */
export * from '../../features/settings/data/subsidiaries.service';
