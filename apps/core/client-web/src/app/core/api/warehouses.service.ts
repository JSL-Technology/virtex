/**
 * Backward-compatibility barrel.
 *
 * The canonical source is `features/masters/data/warehouses.service.ts`.
 * Update existing imports to that path when touching a file.
 */
export * from '../../features/masters/data/warehouses.service';
