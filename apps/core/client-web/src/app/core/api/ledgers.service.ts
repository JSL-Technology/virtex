/**
 * Backward-compatibility barrel.
 *
 * The canonical source is `features/accounting/data/ledgers.service.ts`.
 * Update existing imports to that path when touching a file.
 */
export * from '../../features/accounting/data/ledgers.service';
