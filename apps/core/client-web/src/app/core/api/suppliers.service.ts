/**
 * Backward-compatibility barrel.
 *
 * The canonical source is `features/contacts/data/suppliers.service.ts`.
 * Update existing imports to that path when touching a file.
 */
export * from '../../features/contacts/data/suppliers.service';
