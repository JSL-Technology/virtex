/**
 * Backward-compatibility barrel.
 *
 * The canonical source is `features/reports/data/financial-reporting.service.ts`.
 * Update existing imports to that path when touching a file.
 */
export * from '../../features/reports/data/financial-reporting.service';
