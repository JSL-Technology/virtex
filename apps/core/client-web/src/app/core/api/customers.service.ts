/**
 * Backward-compatibility barrel.
 *
 * The canonical source is `features/contacts/data/customers.service.ts`.
 * Update existing imports to that path when touching a file.
 */
export * from '../../features/contacts/data/customers.service';
